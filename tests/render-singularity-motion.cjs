const fs=require('fs');const source=['scr_defender','scr_effects'].map(n=>fs.readFileSync(`scripts/${n}/${n}.gml`,'utf8')).join('\n').replace(/\bmod\b/g,'%');
const hosts=['triage-preview-host','singularity-preview-host'].map(n=>fs.readFileSync('tests/'+n+'.cjs','utf8')).join('\n');
fs.writeFileSync('docs/singularity-motion.html',`<!doctype html><meta charset="utf-8"><title>SINGULARITY motion study</title><style>body{background:#100c17;color:#e4caff;font:16px system-ui}main{max-width:1100px;margin:auto;padding:20px}canvas{width:100%;border:1px solid #5d417b}button{background:#b982ef;border:0;padding:10px 24px}input{accent-color:#b982ef}label{margin-left:20px}</style><main><h1>SINGULARITY</h1><p>Gravity Well, physical debris and Event Horizon. Game model and effect functions.</p><canvas id="view" width="1100" height="650"></canvas><p><button id="pause">Pause</button><label>View <input id="angle" type="range" min="0" max="360" value="320"></label><label>Scale <input id="zoom" type="range" min=".72" max="5" step=".01" value="4"></label></p><input aria-label="Animation time" id="scrub" type="range" min="0" max="14" step=".01" value="0" style="width:100%"></main><script>${hosts}\nconst p=createSingularityPreview(document.getElementById('view').getContext('2d'),${JSON.stringify(source)});let clock=0,last=0,paused=false;const scrub=document.getElementById('scrub');document.getElementById('pause').onclick=()=>{paused=!paused;document.getElementById('pause').textContent=paused?'Play':'Pause';};scrub.oninput=()=>clock=+scrub.value;function frame(now){if(!paused)clock+=Math.min(.05,(now-last)/1000||0);last=now;p.frame(clock,+document.getElementById('angle').value,+document.getElementById('zoom').value);scrub.value=clock%14;requestAnimationFrame(frame);}requestAnimationFrame(frame);</script>`);
if(process.argv.includes('--render')){const {createCanvas}=require('@napi-rs/canvas');const {createSingularityPreview}=require('./singularity-preview-host.cjs');const c=createCanvas(1100,650),p=createSingularityPreview(c.getContext('2d'),source),sheet=createCanvas(1100,975),ctx=sheet.getContext('2d');[.7,3.1,3.57,3.75,6.5,8.4].forEach((t,i)=>{p.frame(t);ctx.drawImage(c,(i%2)*550,Math.floor(i/2)*325,550,325);});fs.writeFileSync('docs/singularity-effects.png',sheet.toBuffer('image/png'));}
console.log('Rendered SINGULARITY motion study.');
